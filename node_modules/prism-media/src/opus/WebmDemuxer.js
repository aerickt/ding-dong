const WebmBaseDemuxer = require(***REMOVED***../core/WebmBase***REMOVED***);

const OPUS_HEAD = Buffer.from([...***REMOVED***OpusHead***REMOVED***].map(x => x.charCodeAt(0)));

/**
 * Demuxes a Webm stream (containing Opus audio) to output an Opus stream.
 * @extends core.WebmBaseDemuxer
 * @memberof opus
 * @example
 * const fs = require(***REMOVED***fs***REMOVED***);
 * const file = fs.createReadStream(***REMOVED***./audio.webm***REMOVED***);
 * const demuxer = new prism.opus.WebmDemuxer();
 * const opus = file.pipe(demuxer);
 * // opus is now a ReadableStream in object mode outputting Opus packets
 */
class WebmDemuxer extends WebmBaseDemuxer {
  _checkHead(data) {
    if (!data.slice(0, 8).equals(OPUS_HEAD)) {
      throw Error(***REMOVED***Audio codec is not Opus!***REMOVED***);
    }
  }
}

module.exports = WebmDemuxer;
