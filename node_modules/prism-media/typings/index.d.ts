import { Transform } from ***REMOVED***stream***REMOVED***;

import { ChildProcess } from ***REMOVED***child_process***REMOVED***;
import { Duplex } from ***REMOVED***stream***REMOVED***;

import { opus } from ***REMOVED***./opus***REMOVED***;
import { vorbis } from ***REMOVED***./vorbis***REMOVED***;

export interface FFmpegOptions {
  args?: string[];
}

export interface FFmpegInfo {
  command: string;
  info: string;
  version: string;
}

export class FFmpeg extends Duplex {
  public process: ChildProcess;
  constructor(options?: FFmpegOptions);
  static getInfo(force?: boolean): FFmpegInfo;
}

export interface VolumeOptions {
  type: ***REMOVED***s16le***REMOVED*** | ***REMOVED***s16be***REMOVED*** | ***REMOVED***s32le***REMOVED*** | ***REMOVED***s32be***REMOVED***,
  volume?: number
}

export class VolumeTransformer {
  public volume: number;

  constructor(options: VolumeOptions);
  public setVolume(volume: number): void;
  public setVolumeDecibels(db: number): void;
  public setVolumeLogarithmic(value: number): void;
  public readonly volumeDecibels: number;
  public readonly volumeLogarithmic: number;
}

export { opus, vorbis };