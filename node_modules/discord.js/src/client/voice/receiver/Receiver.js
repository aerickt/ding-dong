***REMOVED***use strict***REMOVED***;

const EventEmitter = require(***REMOVED***events***REMOVED***);
const prism = require(***REMOVED***prism-media***REMOVED***);
const PacketHandler = require(***REMOVED***./PacketHandler***REMOVED***);
const { Error } = require(***REMOVED***../../../errors***REMOVED***);

/**
 * Receives audio packets from a voice connection.
 * @example
 * const receiver = connection.createReceiver();
 * // opusStream is a ReadableStream - that means you could play it back to a voice channel if you wanted to!
 * const opusStream = receiver.createStream(user);
 */
class VoiceReceiver extends EventEmitter {
  constructor(connection) {
    super();
    this.connection = connection;
    this.packets = new PacketHandler(this);
    /**
     * Emitted whenever there is a warning
     * @event VoiceReceiver#debug
     * @param {Error|string} error The error or message to debug
     */
    this.packets.on(***REMOVED***error***REMOVED***, err => this.emit(***REMOVED***debug***REMOVED***, err));
  }

  /**
   * Options passed to `VoiceReceiver#createStream`.
   * @typedef {Object} ReceiveStreamOptions
   * @property {string} [mode=***REMOVED***opus***REMOVED***] The mode for audio output. This defaults to opus, meaning discord.js won***REMOVED***t decode
   * the packets for you. You can set this to ***REMOVED***pcm***REMOVED*** so that the stream***REMOVED***s output will be 16-bit little-endian stereo
   * audio
   * @property {string} [end=***REMOVED***silence***REMOVED***] When the stream should be destroyed. If `silence`, this will be when the user
   * stops talking. Otherwise, if `manual`, this should be handled by you.
   */

  /**
   * Creates a new audio receiving stream. If a stream already exists for a user, then that stream will be returned
   * rather than generating a new one.
   * @param {UserResolvable} user The user to start listening to.
   * @param {ReceiveStreamOptions} options Options.
   * @returns {ReadableStream}
   */
  createStream(user, { mode = ***REMOVED***opus***REMOVED***, end = ***REMOVED***silence***REMOVED*** } = {}) {
    user = this.connection.client.users.resolve(user);
    if (!user) throw new Error(***REMOVED***VOICE_USER_MISSING***REMOVED***);
    const stream = this.packets.makeStream(user.id, end);
    if (mode === ***REMOVED***pcm***REMOVED***) {
      const decoder = new prism.opus.Decoder({ channels: 2, rate: 48000, frameSize: 960 });
      stream.pipe(decoder);
      return decoder;
    }
    return stream;
  }
}

module.exports = VoiceReceiver;
