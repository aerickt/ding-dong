***REMOVED***use strict***REMOVED***;

module.exports = (client, packet) => {
  client.emit(***REMOVED***debug***REMOVED***, `[VOICE] received voice server: ${JSON.stringify(packet)}`);
  client.voice.onVoiceServer(packet.d);
};
