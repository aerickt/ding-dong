***REMOVED***use strict***REMOVED***;

const BaseClient = require(***REMOVED***./BaseClient***REMOVED***);
const Webhook = require(***REMOVED***../structures/Webhook***REMOVED***);

/**
 * The webhook client.
 * @implements {Webhook}
 * @extends {BaseClient}
 */
class WebhookClient extends BaseClient {
  /**
   * @param {Snowflake} id ID of the webhook
   * @param {string} token Token of the webhook
   * @param {ClientOptions} [options] Options for the client
   * @example
   * // Create a new webhook and send a message
   * const hook = new Discord.WebhookClient(***REMOVED***1234***REMOVED***, ***REMOVED***abcdef***REMOVED***);
   * hook.send(***REMOVED***This will send a message***REMOVED***).catch(console.error);
   */
  constructor(id, token, options) {
    super(options);
    Object.defineProperty(this, ***REMOVED***client***REMOVED***, { value: this });
    this.id = id;
    Object.defineProperty(this, ***REMOVED***token***REMOVED***, { value: token, writable: true, configurable: true });
  }
}

Webhook.applyToClass(WebhookClient);

module.exports = WebhookClient;
