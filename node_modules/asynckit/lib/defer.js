module.exports = defer;

/**
 * Runs provided function on next iteration of the event loop
 *
 * @param {function} fn - function to run
 */
function defer(fn)
{
  var nextTick = typeof setImmediate == ***REMOVED***function***REMOVED***
    ? setImmediate
    : (
      typeof process == ***REMOVED***object***REMOVED*** && typeof process.nextTick == ***REMOVED***function***REMOVED***
      ? process.nextTick
      : null
    );

  if (nextTick)
  {
    nextTick(fn);
  }
  else
  {
    setTimeout(fn, 0);
  }
}
